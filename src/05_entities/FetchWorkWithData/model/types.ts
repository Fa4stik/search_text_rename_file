const fetchGetFiles = {
    "files": [
        "string"
    ]
}

export type TGetFilesResp = typeof fetchGetFiles

const fetchGetFolders = {
    "folders": [
        "string"
    ]
}

export type TGetFoldersResp = typeof fetchGetFolders